module.exports = {
  i18n: {
    locales: ['en', 'ar'],
    defaultLocale: 'en',
  },
  // webpack5: true,
  // webpack: (config) => {
  //   config.resolve.fallback = { fs: false }

  //   return config
  // },
}
